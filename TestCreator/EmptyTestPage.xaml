<Page x:Class="TestCreator.EmptyTestPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:TestCreator" xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="EmptyTestPage">

    <Grid>
        <materialDesign:Flipper
            materialDesign:FlipperAssist.UniformCornerRadius="4"
        >
            <materialDesign:Flipper.FrontContent>
                <Button
                  Width="184"
                  Margin="8"
                  Command="{x:Static materialDesign:Flipper.FlipCommand}"
                  Content="Нажми!"
                  Style="{StaticResource MaterialDesignFlatButton}"
                />
            </materialDesign:Flipper.FrontContent>
            <materialDesign:Flipper.BackContent>
                <Button
                  Width="184"
                  Margin="8"
                  Command="{x:Static materialDesign:Flipper.FlipCommand}"
                  Content="Test not found ¯\_(ツ)_/¯"
                  Style="{StaticResource MaterialDesignFlatButton}"
                />
            </materialDesign:Flipper.BackContent>
        </materialDesign:Flipper>
    </Grid>
</Page>
